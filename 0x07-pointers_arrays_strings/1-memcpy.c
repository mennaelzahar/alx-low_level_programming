#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _memcpy - check the code
 * @dest: string
 * @src: string
 * @n: int
 * Return: char *.
 * Description: setting to 0
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	return (memcpy(s, b, n));
}
