#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strchr - check the code
 * @s: string
 * @c: char
 * Return: char *.
 * Description: setting to 0
 */
char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}
