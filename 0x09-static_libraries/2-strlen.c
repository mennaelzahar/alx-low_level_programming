#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _strlen - check the code.
 * @s: pointer to char
 * Return: int
 * Description: 'return string size'
 */

int _strlen(char *s)
{
	return (strlen(s));
}
